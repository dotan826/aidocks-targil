import { TestBed } from '@angular/core/testing';

import { ListAndSearchService } from './list-and-search.service';

describe('ListAndSearchService', () => {
  // let service: ListAndSearchService;
  //
  // beforeEach(() => {
  //   TestBed.configureTestingModule({});
  //   service = TestBed.inject(ListAndSearchService);
  // });
  //
  // it('should be created', () => {
  //   expect(service).toBeTruthy();
  // });
});
